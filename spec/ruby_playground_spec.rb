require "spec_helper"

describe RubyPlayground do
  it "has a version number" do
    expect(RubyPlayground::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
